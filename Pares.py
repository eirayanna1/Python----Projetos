# Loop para iterar sobre os números de 1 a 49
for c in range(1, 50):
    # Verifica se o número atual é par usando o operador de módulo (%)
    if c % 2 == 0:
        print(c)  # Se o número for par, imprime-o
